name: Mi Primer Workflow


on:
  push:
    branches:
      - main

jobs:
  ejemplo:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar API Key (No se mostrara por seguridad)
        run: echo "API Key: ${{ secrets.API_KEY }}"